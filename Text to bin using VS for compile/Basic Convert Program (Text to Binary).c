#include<stdio.h>

int main()
{
	char inputString[1000];
    printf("WELCOME TO THE TEXT TO BINARY CONVERTING MACHINE PLEASE INPUT THE TEXT THAT YOU COULD LIKE TO CONVERT(LESS THAN 1000 CHARACTERS): ");
	fgets(inputString, 1000, stdin);
	size_t stringSize = strlen(inputString);
	
}

char charToBin(const char *c){
	
}